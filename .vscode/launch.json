{
    "version": "0.2.0",
    "configurations": [
        {
            "name":"Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/build/Debug/ChessGame.exe",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "build"
        }
    ]
}